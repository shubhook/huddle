# Copy to apps/web/.env if you need non-default API host.
# Bun inlines BUN_PUBLIC_* into the client bundle.

BUN_PUBLIC_API_URL=http://localhost:3000

# Optional override; default derives ws(s):// from BUN_PUBLIC_API_URL
# BUN_PUBLIC_WS_URL=ws://localhost:3000
